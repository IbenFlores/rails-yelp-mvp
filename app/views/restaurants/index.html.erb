<ul>
  <% @restaurants.each do |rest| %>
    <li>
      <%= link_to rest.name, restaurant_path(rest) %>
      <p><%=rest.address %></p>
      <p><%=rest.phone_number %></p>
      <p><%=rest.category %></p>
    </li>
  <% end %>
</ul>
